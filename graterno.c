#import<stdio.h>
int main() {
    int a,b,c;
    printf("enetr first no. :");
    scanf("%d",&a);
    // printf("enter econd no.:");
    // scanf("%d",&b);
    // printf("enter second no.:");
    // scanf("%d",c);
    if (a%2==0) {
        printf(" even no ");
    }
    else if (a%2==0) {
        printf(" neutral no ");

    }
    else 
    {
        printf(" odd no ");
    }
return 0;
}