#include<stdio.h>
int main() {
    int a,b,mul;
    printf("enter two numbers to  multiply :");
    scanf("%d%d",&a,&b);
    mul=a*b;
    printf("Product of two numbers is :%d",mul);
    return 0;
}