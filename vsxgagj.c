#include<stdio.h>
int main() {
    // how to hide password in c
    char password[10];
    printf("Enter password: ");
    scanf("%s", password);
    printf("Password is: %s", password);
    return 0;

}
